interface:
  display_name: "image-prompt-architect"
  short_description: "Design, compress, and repair AI image prompts"
  default_prompt: "Use $image-prompt-architect to turn my image idea or failed output into a clean, model-adapted prompt."
  brand_color: "#2F6B5F"
policy:
  allow_implicit_invocation: true
